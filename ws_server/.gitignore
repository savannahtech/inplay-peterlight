# Node Modules
# Dependencies that are installed via npm or yarn, not needed in version control.
node_modules/

# Logs
# Log files generated during development or production.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# Environment Variables
# Sensitive environment variables used in the project.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Binaries
# Files generated by package manager or during builds.
*.dll
*.exe

# Production Builds
# Build artifacts that are generated during production builds, which can be regenerated.
dist/
build/

# Coverage Directory
# Reports generated by testing frameworks.
#coverage/

# Temporary Files and Folders
# System files or temp files created during development.
*.tmp
*.temp
tmp/
temp/

# MacOS Files
.DS_Store
.AppleDouble
.LSOverride

# IDE/Editor Config Files
# Editor-specific configurations (add your editor's specific files if necessary).
.idea/
.vscode/
*.sublime-workspace
*.sublime-project

# TypeScript
# TypeScript-related artifacts (if using TypeScript with Node/Express).
*.tsbuildinfo
*.d.ts

# Babel
# Babel cache (if using Babel to transpile the project).
.babelrc
.babelrc.js
.babelcache.json
.parcel-cache
.cache/

# Lint Cache
# ESLint or TSLint cache (if using linters in the project).
.eslintcache
.stylelintcache

# Dependency directories for packaging
# Files for specific OS package management or app generation tools.
.nyc_output/
.pnpm-store/
out/
public/

# Optional npm/Yarn lockfiles
# These can be included depending on your project setup. Usually, lockfiles are committed.
yarn.lock
.pnpm-lock.yaml

# Firebase
# Exclude Firebase files if using Firebase for your project.
.firebase/

# AWS
# Exclude AWS credentials files (only if stored locally, though it's not recommended).
.aws/
aws-exports.js

# Other
# Any other custom files generated or unnecessary for version control.
*.swp
*.bak
*.orig
*.swo
