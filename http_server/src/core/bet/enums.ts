export enum BetTypeEnum {
    WINNER = "winner",
    SCORE_EXACT = "scoreExact",
}

export enum BetPickEnum {
    HOME = "home",
    AWAY = "away",
    DRAW = "2-1"
}

export enum BetStateEnum {
    PENDING = "pending",
    LOSS = "loss",
    WIN = "win"
}